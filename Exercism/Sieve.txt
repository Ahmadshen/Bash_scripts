#!/usr/bin/env bash

using famous "Sieve of Eratosthenes" algorithm,
find prime numbers

Prime number $expected == 1 && input==$expected

To use the Sieve of Eratosthenes, you first create a list of all the numbers between 2 and your given number

unmarked number == prime number
multiples of prime number == composit number && != prime numbers

assert = $expected