#!/usr/bin/env bash

psuedo code

argumet $1
if argument is divisible by 4 and 400 echo true
if argumnent is divisible by 4 but not 400 echo false
if argument is divisible by 400 but not 4 echo false
if multiple arguments put by user then echo "Usage: leap.sh <year>"
if alhabets [a-z] then echo "Usage: leap.sh <year>"
